name: E2E Workflow

on: [push]

jobs:
  e2e-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Start containers en voer tests uit
        run: |
          docker compose -f docker-compose-test.yaml up --abort-on-container-exit --exit-code-from test-runner